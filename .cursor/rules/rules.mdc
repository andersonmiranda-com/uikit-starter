---
description: UI Kit rules for the e-commerce project
globs: 
alwaysApply: true
---
# Rules for the UI Kit Project

## File Structure

- Components must be in the `lib/` directory
- Each component must have its own folder and include:
  - The component file
  - A story file
  - A unit test file

## Naming Conventions

- Components should use PascalCase (e.g., Button.tsx)
- Story files should follow the pattern ComponentName.stories.tsx
- Interfaces should use PascalCase (e.g., ButtonProps)
- Variables and functions should use camelCase

## React Components

- Components must be implemented using native HTML elements as the base (e.g., <button>, <input>, <a>, etc.).
- Extend the original attributes of the corresponding HTML element using React interfaces (e.g., React.ButtonHTMLAttributes for buttons).
- The props interface must include all variants and behaviors detected in the Figma design.
- Keep components functional and small
- Define TypeScript types for all props
- Document important props using JSDoc
- For more complex components, create partials in a `partials/` sub-directory

## Styling

- Use Tailwind CSS for styling
- Keep Tailwind classes organized and reusable
- Use tailwind-variants (tv) to manage component states and variants
- Define custom variants that complement React Aria functionality
- Use utilities like focusRing for a consistent user experience
- Provide default variant values using defaultVariants in tv()
- Avoid inline styles

## Testing

- Write unit tests for components
- Test files should follow the pattern ComponentName.spec.tsx
- Use Vitest for testing
- Maintain good test coverage

## Documentation

- Document important props with JSDoc
- Keep README.md up to date
- Provide usage examples in Storybook stories
- Stories and Documentation are in English

## Git

- Make atomic commits
- Use descriptive commit messages
- Keep .gitignore updated

## TypeScript

- Use strict types
- Avoid `any`
- Define interfaces for all props
- Use type inference when possible

## Accessibility

| Requirement                  | Description                                                            |
| ---------------------------- | ---------------------------------------------------------------------- |
| Semantic HTML tags           | Use appropriate HTML tags for content structure and meaning.           |
| Keyboard navigation          | All functionality must be accessible via keyboard.                     |
| Focus management             | Focus must be managed properly to facilitate navigation.               |
| ARIA attributes              | Include ARIA attributes where necessary to enhance accessibility.      |
| Screen reader support        | The interface must be usable with screen readers.                      |
| Adequate color contrast      | Ensure sufficient contrast between text and background.                |
| Visible focus indicators     | Focus indicators must be clear and consistent on all elements.         |
| Accessible error messages    | Error messages must be clear and announced by assistive technologies.  |
| Adjustable font size         | Allow users to adjust the font size.                                   |
| Images with alternative text | All images must have descriptive alternative text.                     |
| Accessible forms             | Forms must have associated labels and clear descriptions.              |
| Avoid flashing content       | Do not include flashing or blinking content to prevent epilepsy risks. |

---

## Performance

- Avoid unnecessary re-renders
- Use React.memo when appropriate
- Optimize imports

## Dependencies

- Use yarn to install dependencies

## Implementations

- Implementations must pass linting

